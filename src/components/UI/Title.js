import styled from "styled-components";
import { margin } from "../../styles/mixin/mixin.styled";

const Title = styled.h1`
    ${margin};
    font-size: 2.5rem;
    line-height: 100%;
    text-align: center;
    font-weight: 400;
`;

export default Title;
